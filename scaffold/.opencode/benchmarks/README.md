# llmzk regression benchmarks

These cases check an existing generated vault. They do not run an LLM.

Run from the installed vault root:

```bash
.opencode/bin/llmzk benchmark .opencode/benchmarks --vault .
```
