---
description: Show Git safety status for the llmzk vault
---

Use the **fast operating profile** from `.opencode/docs/OPERATING_PROFILES.md`.
Use RTK for token-efficient Git inspection if available. Otherwise run:

```bash
.opencode/bin/llmzk git status .
```
