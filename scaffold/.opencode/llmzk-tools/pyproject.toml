[project]
name = "llmzk-tools"
version = "0.6.1"
description = "Small Python utilities for the llmzk OpenCode/Obsidian harness"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.2",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
